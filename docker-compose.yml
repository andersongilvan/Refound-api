services:
  postgres:
    container_name: db-refponds

    image: bitnami/postgresql:latest

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER : admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: db_refounds